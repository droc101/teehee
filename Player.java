public class Player {

    // Yet another class that should be a struct, but java is stupid

    Vector2 position = new Vector2(0,0);
    double rotation = 0;

    int health = 10;
    int ammo = 0;

    int keys = 0;
}
